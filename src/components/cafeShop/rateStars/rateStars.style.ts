import styled from 'styled-components';

const RatesBox = styled.div`
  display: flex;
  align-items: center;
  font-size: 14px;
  color: #383b40;
`;

const RateIconsBox = styled.div`
  margin-right: 5px;
`;

export default { RatesBox, RateIconsBox };
